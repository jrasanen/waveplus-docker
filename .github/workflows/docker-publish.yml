name: Build and Push to Docker Hub

on:
  workflow_dispatch:
  push:
    branches:
      - trunk
  schedule:
    - cron: '0 */6 * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    env:
      DOCKERHUB_REPO: jrasanen/waveplus-docker
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Fetch the latest waveplus release tag
      id: waveplus_release
      run: |
        LATEST_RELEASE=$(curl -s "https://api.github.com/repos/alepar/waveplus/tags" | jq -r '.[0].name')
        echo "Latest release tag is $LATEST_RELEASE"
        echo "latest_release=$LATEST_RELEASE" >> $GITHUB_OUTPUT
        echo "::set-output name=tag::$LATEST_RELEASE"
      env:
        GITHUB_TOKEN: ${{ secrets.A_GITHUB_TOKEN }}

    - name: Check if the tag exists on Docker Hub
      id: dockerhub_check
      run: |
        TAG=${{ steps.waveplus_release.outputs.tag }}
        TAG_EXISTS=$(curl -s -f "https://hub.docker.com/v2/repositories/${DOCKERHUB_REPO}/tags/${TAG}/" > /dev/null; echo $?)
        
        echo "tag_exists=$TAG_EXISTS" >> $GITHUB_OUTPUT

        if [ "$TAG_EXISTS" == "0" ]; then
          echo "Tag $TAG exists on Docker Hub. Exiting workflow."
        fi

    - name: Check out waveplus repository at the latest tag
      if: steps.dockerhub_check.outputs.tag_exists != '0'
      uses: actions/checkout@v4
      with:
        repository: alepar/waveplus
        ref: ${{ steps.waveplus_release.outputs.tag }}
        path: "waveplus"

    - name: Copy our Dockerfile into waveplus context
      if: steps.dockerhub_check.outputs.tag_exists != '0'
      run: cp Dockerfile waveplus/Dockerfile

    - name: Copy our Entrypoint into waveplus context
      if: steps.dockerhub_check.outputs.tag_exists != '0'
      run: cp entrypoint.sh waveplus/entrypoint.sh

    - name: Set up Docker Buildx
      if: steps.dockerhub_check.outputs.tag_exists != '0'
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      if: steps.dockerhub_check.outputs.tag_exists != '0'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and Push
      if: steps.dockerhub_check.outputs.tag_exists != '0'
      uses: docker/build-push-action@v5
      with:
        context: ./waveplus
        file: ./waveplus/Dockerfile
        push: true
        tags: |
          ${{ env.DOCKERHUB_REPO }}:${{ steps.waveplus_release.outputs.tag }}
          ${{ env.DOCKERHUB_REPO }}:latest
        platforms: linux/amd64,linux/arm64
